name: code_secure_flutter_example
description: "An example project for the code_secure_flutter package."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  

dev_dependencies:
  custom_lint: ^0.7.3
  code_secure_flutter: 
    path: ../code_secure_flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true